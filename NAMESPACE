useDynLib(DECIPHER)

import(methods)
import(IRanges)
import(XVector)
import(Biostrings)
import(RSQLite)

export(
# interacting with a database:
Add2DB, Seqs2DB, SearchDB, DB2Seqs,
# assigning an identifier:
FormGroups, IdentifyByRank,
# lengths:
IdLengths, TerminalChar,
# grouping sequences:
DistanceMatrix, IdClusters, MODELS,
# forming consensus:
ConsensusSequence, IdConsensus, TileSeqs,
# chimeras:
FindChimeras, CreateChimeras,
# microarrays:
DesignArray, Array2Matrix, NNLS, CalculateEfficiencyArray,
# primers:
CalculateEfficiencyPCR, DesignPrimers,
# probes:
CalculateEfficiencyFISH, DesignProbes,
# viewing information:
BrowseSequences, BrowseDB,
# multiple alignment
AlignSeqs, AlignProfiles, AlignTranslation, MaskAlignment, AlignDB
)
