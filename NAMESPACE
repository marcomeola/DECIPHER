useDynLib(DECIPHER)

import(Biostrings)
import(IRanges)

export(
# interacting with a database:
Add2DB, Seqs2DB, SearchDB, DB2FASTA,
# assigning an identifier:
FormGroups, IdentifyByRank,
# lengths:
IdLengths, TerminalChar,
# grouping sequences:
DistanceMatrix, IdClusters, MODELS,
# forming consensus:
ConsensusSequence, IdConsensus, TileSeqs,
# chimeras:
FindChimeras, CreateChimeras,
# microarrays:
DesignArray, Array2Matrix, NNLS, CalculateEfficiencyArray,
# primers:
CalculateEfficiencyPCR, DesignPrimers,
# viewing information:
BrowseSequences, BrowseDB,
# multiple alignment
AlignSeqs, AlignProfiles, MaskAlignment
)
